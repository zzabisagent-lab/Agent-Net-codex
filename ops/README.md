# Ops bootstrap

This directory is reserved for deployment and runtime assets.

Per the M01 guide, production deployment configuration is out of scope for
this module. Dockerfiles, reverse-proxy config, backup scripts, and restore
scripts should be added in M17.
